import type { ReactNode } from "react";
import "@/app/styles/location-pages.css";
import "@/app/styles/mississauga-page.css";

export default function CityLayout({ children }: { children: ReactNode }) {
  return children;
}
